{
    "default-address-pools":
        [
            {
                "base": "{{ address }}",
                "size": {{ size }}
            }
        ],
    "registry-mirrors": ["{{ mirrors }}"]
}
