C0='\[\e[0m\]'
C1='\[\e[1;31m\]'
C2='\[\e[1;32m\]'
C3='\[\e[1;33m\]'
C6='\[\e[1;36m\]'
if [ $(id -u) -eq 0 ]; then # you are root, make the prompt red
    PS1="${debian_chroot:+($debian_chroot)}${C1}\\u@{{ inventory_hostname }}${C6}:\\w${C0}\\$ "
else
    PS1="${debian_chroot:+($debian_chroot)}${C2}\\u@{{ inventory_hostname }}${C6}:\\w${C0}\\$ "
fi
